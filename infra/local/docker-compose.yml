version: "3.9"
services:
  postgres:
    image: postgres:16
    container_name: psms-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: psms
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
